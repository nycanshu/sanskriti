import 'package:flutter/material.dart';

class EducationAdmin extends StatelessWidget {
  const EducationAdmin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('No content available for this category'),
      ),
    );
  }
}
